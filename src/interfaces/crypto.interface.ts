export interface CryptoData {
  symbol: string;
  open: number;
  high: number;
  low: number;
  close: number;
  lastPrice: number;
  prevHigh: number[];
  prevLow: number[];
  priceChange: number;
}
